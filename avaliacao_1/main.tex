\documentclass[12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[portuguese]{babel}
\usepackage{amsmath, amssymb}
\usepackage{graphicx}
\usepackage[margin=2.5cm]{geometry}

\title{1ª Avaliação - Processamento Digital de Sinais}
\author{Ana Iara Loayza Costa, José Victor Brito Costa e Milena Freire Britto Neves}
\date{28/07/2026}

\begin{document}
\maketitle

\section*{Questão 1}

\subsection*{a) }

Um sinal senoidal discreto $x[n] = A\cos(\omega_0 n + \theta)$ é periódico se, e somente se,
existir $N \in \mathbb{Z}^+$ e $r \in \mathbb{Z}$ tais que:
\[
\omega_0 N = 2\pi r
\]

Para o sinal dado, $\omega_0 = \dfrac{12\pi}{5}$ e $\theta = 0$. Substituindo:
\[
\frac{12\pi}{5}N = 2\pi r \;\Rightarrow\; N = \frac{5r}{6}
\]

O menor valor inteiro de $r$ que torna $N$ inteiro é $r = 6$, resultando em:
\[
N = \frac{5 \cdot 6}{6} = 5
\]

Portanto, $x[n]$ é periódico, com período fundamental $N = 5$.

\bigskip
\noindent\textit{Gráfico comprovando a periodicidade:}

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.7\textwidth]{images/grafico_1a.png}
\end{figure}

\subsection*{b) }

\textbf{Multiplicando por 2:} $\omega_0' = \dfrac{24\pi}{5}$
\[
\frac{24\pi}{5}N = 2\pi r \;\Rightarrow\; N = \frac{5r}{12}
\]
Menor $r$ inteiro válido: $r = 12 \Rightarrow N = 5$.

 O período permanece $N = 5$, mesmo dobrando a frequência. Isso explica uma
propriedade importante dos sinais discretos: diferentemente do caso contínuo
($T = 2\pi/f$, inversamente proporcional à frequência), no caso discreto essa
relação não é garantida.

\textbf{Multiplicando por 5:} $\omega_0'' = 12\pi$
\[
12\pi N = 2\pi r \;\Rightarrow\; N = \frac{r}{6}
\]
Menor $r$ inteiro válido: $r = 6 \Rightarrow N = 1$. Como $12\pi n$ é sempre múltiplo inteiro de $2\pi$, temos $\cos(12\pi n) = 1$
para todo $n$, ou seja, o sinal se torna constante (período trivial $N=1$).

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.7\textwidth]{images/grafico_1b_x2.png}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.7\textwidth]{images/grafico_1b_x5.png}
\end{figure}

\section*{Questão 2}

Foi implementada, em Python, a função \texttt{Deslocamento\_Tempo(x, n, n0)}, que
recebe o sinal $x$, seu vetor de índices $n$ e o escalar de deslocamento $n_0$, e
retorna o vetor $y$ tal que
\[
y[n] = x[n - n_0].
\]

Para cada índice $n_i$ do vetor $n$, a função busca o valor de $x$ correspondente
ao índice $n_i - n_0$ dentro do domínio original de $x$; caso esse índice não
exista nesse domínio, o valor retornado é 0. Dessa forma, $y$ fica definido sobre
o \textbf{mesmo} vetor de índices $n$ recebido como parâmetro, permitindo plotar
$x$ e $y$ diretamente no mesmo eixo, como pedido no enunciado. A implementação é
válida para qualquer vetor $x$, vetor de índices $n$ (não necessariamente
começando em 0 ou contíguo) e escalar $n_0$ (positivo, negativo ou nulo).

\begin{verbatim}
def Deslocamento_Tempo(x, n, n0):
    x = np.asarray(x)
    n = np.asarray(n)
    y = np.zeros_like(x, dtype=float)
    for i, ni in enumerate(n):
        alvo = ni - n0
        pos = np.where(n == alvo)[0]
        if pos.size > 0:
            y[i] = x[pos[0]]
        else:
            y[i] = 0
    return y
\end{verbatim}

\subsection*{Exemplo}

Como exemplo, considerou-se $x[n]$ um pulso triangular definido para $n \in [-3, 5]$,
com deslocamento $n_0 = 3$ (atraso de 3 amostras). O gráfico abaixo mostra $x[n]$ e
$y[n]$ no mesmo eixo $n$, com legenda identificando cada sinal — note os zeros nas
bordas onde o sinal deslocado ainda não "entrou" no domínio observado.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.7\textwidth]{images/grafico_2.png}
    \caption{Deslocamento no tempo: $x[n]$ e $y[n] = x[n-3]$}
\end{figure}

\section*{Questão 3}

Para um sistema linear e invariante no tempo, a saída é dada pela convolução
entre o sinal de entrada $x[n]$ e a resposta ao impulso $h[n]$:
\[
y[n]=(x*h)[n]=\sum_{k=-\infty}^{\infty}x[k]h[n-k].
\]

Como o enunciado estabelece que ambos os sinais são causais,
$x[n]=h[n]=0$ para $n<0$. Assim, o somatório pode ser limitado a
\[
y[n]=\sum_{k=0}^{n}x[k]h[n-k].
\]

Se $x$ possui $N_x$ amostras e $h$ possui $N_h$ amostras, a convolução completa
possui
\[
N_y=N_x+N_h-1
\]
amostras. Na implementação, cada produto $x[k]h[m]$ contribui para a amostra
de índice $n=k+m$:
\[
y[k+m]\leftarrow y[k+m]+x[k]h[m].
\]
Esse procedimento implementa diretamente a definição, sem usar funções prontas
de convolução.

\begin{verbatim}
def Convolucao(x, h):
    x = np.asarray(x)
    h = np.asarray(h)

    if x.ndim != 1 or h.ndim != 1:
        raise ValueError("x e h devem ser vetores unidimensionais.")
    if x.size == 0 or h.size == 0:
        raise ValueError("x e h não podem ser vetores vazios.")

    tipo = np.result_type(x.dtype, h.dtype)
    y = np.zeros(x.size + h.size - 1, dtype=tipo)

    for k in range(x.size):
        for m in range(h.size):
            y[k + m] += x[k] * h[m]

    return y
\end{verbatim}

\subsection*{Exemplo}

Sejam
\[
x[n]=\{1,2,3\}
\qquad\text{e}\qquad
h[n]=\left\{1,\frac{1}{2}\right\},
\]
com a primeira amostra de cada vetor correspondendo a $n=0$. Então:
\[
\begin{aligned}
y[0]&=x[0]h[0]=1,\\
y[1]&=x[0]h[1]+x[1]h[0]=\frac{1}{2}+2=\frac{5}{2},\\
y[2]&=x[1]h[1]+x[2]h[0]=1+3=4,\\
y[3]&=x[2]h[1]=\frac{3}{2}.
\end{aligned}
\]
Portanto,
\[
\boxed{y[n]=\left\{1,\frac{5}{2},4,\frac{3}{2}\right\}}.
\]

\section*{Questão 4}

A equação fornecida pode ser reescrita como
\[
y[n]=(1+r)y[n-1]+x[n].
\]
Definindo $a=1+r$, obtém-se o sistema de primeira ordem
\[
y[n]-ay[n-1]=x[n].
\]

Como o aporte mensal é constante e o primeiro aporte ocorre ao final do primeiro
mês, sua sequência é $x[n]=x\,u[n-1]$. A resposta ao impulso do sistema é
\[
H(z)=\frac{1}{1-az^{-1}}
\qquad\Longleftrightarrow\qquad
h[n]=a^n u[n].
\]

A resposta total é a soma da resposta à condição inicial com a resposta de estado
nulo. Para um saldo inicial $y[0]=y_0$:
\[
y_{\mathrm{inicial}}[n]=a^n y_0.
\]
A parcela causada pelos aportes é calculada por convolução:
\[
\begin{aligned}
y_{\mathrm{aportes}}[n]
  &=\sum_{k=1}^{n}x\,h[n-k] \\
  &=x\sum_{m=0}^{n-1}a^m.
\end{aligned}
\]
Para $r\neq 0$, a soma geométrica resulta em
\[
y_{\mathrm{aportes}}[n]
=x\frac{a^n-1}{a-1}
=x\frac{(1+r)^n-1}{r}.
\]
Portanto, a solução fechada, sem iteração recursiva, é
\[
\boxed{
y[n]=y_0(1+r)^n+x\frac{(1+r)^n-1}{r}
},\qquad r\neq 0.
\]
Quando $r=0$:
\[
\boxed{y[n]=y_0+nx}.
\]

\begin{verbatim}
def Investimento(y0, r, x, n):
    if not isinstance(n, (int, np.integer)) or isinstance(n, (bool, np.bool_)):
        raise TypeError("n deve ser um número inteiro de meses.")
    if n < 0:
        raise ValueError("n não pode ser negativo.")
    if r <= -1:
        raise ValueError("r deve ser maior que -1.")

    if np.isclose(r, 0.0):
        return y0 + n*x

    fator = (1 + r)**n
    soma_aportes = np.expm1(n*np.log1p(r))/r
    return y0*fator + x*soma_aportes
\end{verbatim}

\subsection*{Exemplo}

Para $y_0=1000$, rentabilidade mensal de $1\%$ ($r=0{,}01$), aporte mensal de
$200$ e prazo de $12$ meses:
\[
\begin{aligned}
\mathrm{Investimento}(1000,0{,}01,200,12)
&=1000(1{,}01)^{12}
  +200\frac{(1{,}01)^{12}-1}{0{,}01}\\
&\approx 3663{,}33.
\end{aligned}
\]

\section*{Questão 5}

Dada a equação de diferenças
\[
y[n] = 0{,}038\,x[n-1] + 0{,}034\,x[n-2] + 1{,}63\,y[n-1] - 0{,}70\,y[n-2],
\]
determinam-se a seguir a função de transferência, a resposta em frequência e a
saída para uma entrada senoidal.

\subsection*{a) Função de transferência $H(z)$}

Aplicando a transformada $z$ e usando a propriedade do deslocamento
($x[n-k]\leftrightarrow z^{-k}X(z)$), agrupam-se os termos em $X(z)$ e $Y(z)$:
\[
Y(z)\left(1 - 1{,}63\,z^{-1} + 0{,}70\,z^{-2}\right)
= X(z)\left(0{,}038\,z^{-1} + 0{,}034\,z^{-2}\right).
\]
Isolando a razão entre saída e entrada:
\[
\boxed{
H(z) = \frac{Y(z)}{X(z)}
= \frac{0{,}038\,z^{-1} + 0{,}034\,z^{-2}}{1 - 1{,}63\,z^{-1} + 0{,}70\,z^{-2}}
= \frac{0{,}038\,z + 0{,}034}{z^{2} - 1{,}63\,z + 0{,}70}.
}
\]
As raízes do denominador (polos) são $z = 0{,}815 \pm 0{,}189j$, com módulo
$|z| \approx 0{,}837 < 1$. Como todos os polos estão dentro do círculo unitário,
o sistema é \textbf{estável} e a resposta em frequência está bem definida.

\subsection*{b) Representação de $H(z)$ em função da frequência}

Substituindo $z = e^{j\omega}$ na função de transferência:
\[
H(e^{j\omega}) =
\frac{0{,}038\,e^{-j\omega} + 0{,}034\,e^{-j2\omega}}
     {1 - 1{,}63\,e^{-j\omega} + 0{,}70\,e^{-j2\omega}}.
\]
A \textbf{magnitude} é dada por $\left|H(e^{j\omega})\right|$ e a \textbf{fase}
por $\angle H(e^{j\omega})$, ambas funções da frequência angular $\omega$.

\subsection*{c) Gráficos da magnitude e da fase}

Os gráficos abaixo mostram a magnitude e a fase de $H(e^{j\omega})$ para
$\omega \in [0, \pi]$. Observa-se um comportamento de filtro que atenua as altas
frequências e a região em torno de $\omega = \pi/4$.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.8\textwidth]{images/grafico_5_mag_fase.png}
    \caption{Magnitude e fase da resposta em frequência $H(e^{j\omega})$.}
\end{figure}

\subsection*{d) Saída para $x[n] = \cos\!\left(\frac{\pi}{4}n\right)$}

Para um sistema LIT estável, a resposta em regime permanente a uma senoide
$x[n] = \cos(\omega_0 n)$ é a mesma senoide, escalada em amplitude por
$\left|H(e^{j\omega_0})\right|$ e defasada por $\angle H(e^{j\omega_0})$:
\[
y[n] = \left|H(e^{j\omega_0})\right|\,
\cos\!\big(\omega_0 n + \angle H(e^{j\omega_0})\big).
\]
Com $\omega_0 = \dfrac{\pi}{4}$, avalia-se $H(e^{j\omega})$ nesse ponto:
\[
\left|H(e^{j\pi/4})\right| \approx 0{,}1393,
\qquad
\angle H(e^{j\pi/4}) \approx -3{,}051\ \text{rad}\ (\approx -174{,}8^\circ).
\]
Portanto, a saída em regime permanente é
\[
\boxed{
y[n] \approx 0{,}1393\,\cos\!\left(\frac{\pi}{4}n - 3{,}051\right).
}
\]
O sistema atenua fortemente essa frequência (ganho $\approx 0{,}14$) e introduz
uma defasagem de quase meia volta.

\subsection*{e) Gráfico de $x[n]$ e $y[n]$}

O gráfico a seguir apresenta a entrada $x[n]$ e a saída $y[n]$ no mesmo eixo, com
legendas discriminando os sinais. Nota-se a forte redução de amplitude e o
deslocamento de fase previstos analiticamente.

\begin{figure}[h!]
    \centering
    \includegraphics[width=0.8\textwidth]{images/grafico_5_xy.png}
    \caption{Entrada $x[n]=\cos(\pi n/4)$ e saída $y[n]$ em regime permanente.}
\end{figure}

\end{document}
